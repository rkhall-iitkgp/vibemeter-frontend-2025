export interface FocusGroup{
    id: string;
    title: string;
    createdDate: string;
    description: string;
    tags: string[];
    participantCount: number;
}

export interface Employee {
  name: string;
  id: string;
  title: string;
  dateAdded: string;
}